# Local scripts

`build_ai_candidate_preflight.py` creates a deterministic, name-screened candidate file from the supplied SDC export. Its rows are not qualifying AI transactions until primary-source review adds AI relevance evidence, transaction form, and source URLs.
